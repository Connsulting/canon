---
id: d0cb411
type: technical
title: "Documentation Backfiller"
domain: canon-cli
created: 2026-03-13T08:03:53Z
depends_on: [8cfdb59]
touched_domains: [canon-cli]
---
# Documentation Backfiller Draft Spec

## Problem Statement
README command documentation has drifted from implemented CLI behavior. This causes operators to miss supported commands/flags and rely on incorrect defaults.

## Desired Behavior
Backfill README so command and flag docs match implemented `canon` CLI behavior, including routed `help` and documented root/default handling, and add a unit test that guards critical parity to prevent future drift.

## Scope (Minimal High-Value)
In scope:
1. Update `README.md` command list and options where behavior is already implemented in `cmd/canon/main.go`.
2. Correct known drift for `log --all` default semantics.
3. Add docs for currently implemented but missing items (`help`, `index`, `version --short`, `log --root`, `log --show-tags`, `gc --root`, and related concrete flags needed for parity).
4. Add a unit test that enforces critical README/CLI parity checks.

Out of scope:
1. New CLI features.
2. Full docs site generation or broad prose rewrite.
3. Behavioral changes to CLI command handling.

## Assumptions
1. Routed commands and command flag definitions in `cmd/canon/main.go` are the behavior source of truth for this backfill.
2. README should focus on practical command usage and key options, not internal implementation detail.
3. Critical parity assertions are preferable to brittle full-text snapshot tests.

## Acceptance Criteria
1. README command list includes all user-facing commands currently routed by `run()`, including `help`, `index`, and `version`.
2. README includes docs for `index`, `version --short`, `log --root`, `log --show-tags`, and corrected `log --all` default behavior.
3. README includes `gc --root` and other implemented core GC flags.
4. Unit tests include a dedicated README parity test for critical command/flag/default coverage.
5. `go test ./...` passes.
6. `go vet ./...` passes.
7. Manual CLI checks include one positive and one negative run for this feature.

## Validation Plan
1. `go test ./...`
2. `go vet ./...`
3. Positive CLI check: `go run ./cmd/canon version --short`
4. Negative CLI check: `go run ./cmd/canon does-not-exist`

## Risks / Tradeoffs
1. README parity tests can become too rigid; keep assertions focused on high-risk drift points.
2. Expanding docs too broadly increases maintenance burden; keep edits minimal and source-aligned.
